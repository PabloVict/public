version: 0.2

phases:
  build:
    commands:
      - echo "This is a sample content for testpab.txt" > /tmp/testpab.txt
      - aws s3api put-object \
          --bucket codepipeline4654 \
          --key testpab.txt \
          --body /tmp/testpab.txt \
          --server-side-encryption aws:kms \
          --region us-east-1
